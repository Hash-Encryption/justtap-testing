# JustTap V2 Repository Inventory

Snapshot date: 2026-08-15. “Authoritative” below means the intended V2 migration target, not necessarily production-ready behavior.

| Subsystem              | Current implementation                                                                                                                                                        | Duplicate or legacy implementation                                                                                                | Likely authority                                          | Dependencies                                                           | Principal risks                                                                                                                                | Planned phase      |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| Routing                | TanStack file routes in `src/routes`; generated `src/routeTree.gen.ts`.                                                                                                       | Next App Router under `app/`; `public/_redirects` rewrites `/c/*` to missing `c.html`.                                            | TanStack Start                                            | TanStack Router/Start, Vite                                            | Two routers; stale rewrite; generated tree must not be hand-edited.                                                                            | 01, 14, 17         |
| Homepage               | `src/routes/index.tsx`.                                                                                                                                                       | `app/page.tsx`.                                                                                                                   | TanStack                                                  | React, shared styles                                                   | Product claims may describe unfinished Wallet behavior.                                                                                        | 07, 14             |
| Authentication         | `src/routes/auth.tsx`, `src/hooks/useAuth.ts`, Supabase Auth.                                                                                                                 | `app/auth/page.tsx` and legacy Supabase client.                                                                                   | TanStack                                                  | Supabase Auth, RLS                                                     | Invalid V2 Supabase import is fixed; flows and route protection remain duplicated.                                                             | 04                 |
| Dashboard              | `src/routes/dashboard.tsx` with editor, Pro, analytics, leads, and QR tabs.                                                                                                   | `app/dashboard/page.tsx` and root `components/dashboard/*`.                                                                       | TanStack                                                  | Supabase, storage, local drafts                                        | Broad reads; guest draft auto-publish; duplicated behavior.                                                                                    | 04, 06, 14         |
| Card editor            | `src/components/dashboard/CardEditor.tsx` with explicit insert/update through `card-save.ts`.                                                                                 | `components/dashboard/CardEditor.tsx`.                                                                                            | TanStack                                                  | Supabase cards/storage, sanitization                                   | Phase 01 fixed the undeclared query, owner-scoped update, duplicate UX, and editor `plan_tier` write; full publishing design remains Phase 06. | 06                 |
| Public card routing    | `src/routes/c.$slug.tsx` uses router params, the server-only resolver in `public-card.server.ts`, and crawler-visible card metadata.                                          | Legacy Next public-card routes, client rediscovery, and `c.html` rewrite were removed in Phase 01.                                | TanStack                                                  | Supabase, server function, router loader                               | Public RPC changes must retain the explicit narrow projection and inactive/missing 404 behavior.                                               | 01, 02, 07, 17     |
| Public renderer        | `src/components/card/CardView.tsx` is the shared V2 visual renderer for persisted public cards and CardEditor preview; `card-design.ts` resolves Classic/custom presentation. | Legacy Next public renderer components were removed in Phase 01; the separate Phase 06 preview rendering was retired in Phase 07. | TanStack                                                  | React, narrow public resolver, Phase 06 design fields, vCard/lead APIs | Public views use the narrow best-effort analytics helper; Apple Wallet signing remains unavailable.                                            | 07, 09, 10, 12     |
| Admin                  | `src/routes/admin.tsx`, `src/routes/api/admin-auth.ts`, roles in Supabase schema.                                                                                             | `app/admin/page.tsx`.                                                                                                             | TanStack, after security redesign                         | Supabase roles/cards/profiles, custom admin env values                 | Custom reversible token; privileged browser operations; broad reads; direct tier changes.                                                      | 04, 05, 08         |
| vCard                  | `src/routes/api/vcard.$slug.ts` reuses the active public-card resolver and `buildVCard`; downloads record through `record_public_card_event` with the anonymous key.          | Legacy Next vCard route was removed in Phase 01; `lib/vcard.ts` remains frozen.                                                   | TanStack                                                  | Server route, public resolver, analytics RPC                           | Analytics remains best-effort and never changes vCard response success.                                                                        | 11, 14             |
| Wallet                 | TanStack UI advertises Wallet through Pro/QR surfaces but no equivalent TanStack Wallet API exists.                                                                           | `app/api/wallet/[slug]/route.ts` and `components/dashboard/QrWalletHub.tsx` implement/fallback differently.                       | Undecided until migration design; target must be TanStack | Third-party Wallet API, vCard, signing requirements                    | Split implementation, demo/fallback behavior, product claims ahead of target implementation.                                                   | 12                 |
| Connections            | `CardView` uses the narrow `create_public_connection` RPC with confirmed success analytics; the owner-scoped Connections dashboard lists complete visitor data, quick contact actions, delete, and database-enforced Pro follow-up management/CSV. | Legacy `LeadModal` and `LeadsTab`.                                                                                                | TanStack                                                  | `card_leads`, RLS, analytics RPC, email/webhook APIs                   | Durable abuse controls and notification/webhook hardening remain deferred; public capture and private management stay on separate trust boundaries. | Upgrade 01, 04, 05 |
| Analytics              | Shared `analytics.ts` emitters use narrow public RPCs; the Pro owner dashboard uses aggregate-only `get_owner_card_analytics` for UTC ranges, KPIs, bounded trends, actions, and attributed sources. | `app/api/analytics/route.ts` and legacy analytics tab remain frozen.                                                              | TanStack + Supabase RPC                                   | `card_analytics`, RLS, UUID event/session identity                     | The All trend is intentionally limited to the latest 12 calendar months while its KPIs and breakdown totals remain true all-time.              | Upgrade 02, 03, 05 |
| Email                  | `src/routes/api/lead-email.ts` calls Resend.                                                                                                                                  | No authoritative separate legacy email path found.                                                                                | TanStack server API                                       | Resend, card/pro settings, server secrets                              | Exposed historical key, broad read, recipient/settings trust, webhook side effect.                                                             | 11                 |
| Webhook                | `src/routes/api/lead-webhook.ts`; lead-email route can also dispatch.                                                                                                         | No authoritative Next equivalent found.                                                                                           | TanStack server API                                       | Card Pro settings, outbound fetch                                      | SSRF, duplicate dispatch, no signing/retry/idempotency.                                                                                        | 11                 |
| Supabase client/data   | `src/lib/supabase.ts`, `src/lib/env.ts`, direct browser and server queries.                                                                                                   | `lib/supabase/client.ts` and `server.ts`.                                                                                         | TanStack with explicit server/client boundaries           | Supabase JS, environment config                                        | Broad selects, mixed trusted/untrusted access, public/private fields combined.                                                                 | 01, 02, 04         |
| Database/RLS           | Ordered migrations through `20260815040000_analytics_functional_ui.sql`; `schema.sql` remains a legacy snapshot.                                                              | No migration runner/link is committed.                                                                                            | Supabase through ordered migrations                       | PostgreSQL, Supabase Auth/Storage                                      | Public writes use narrow RPCs; owner analytics aggregation and raw SELECT both derive entitlement from the authoritative profile tier.         | 02-07, Upgrade 01-05 |
| Cloudflare/deployment  | Vite/Nitro builds `dist/_worker.js`; Wrangler targets `dist`; generated routes send dynamic paths to the worker. Phase 07 was verified on the feature-branch staging alias.   | Legacy Next config still describes static export but is no longer used by default build scripts.                                  | Vite/Nitro Cloudflare target                              | Wrangler, Nitro, Lovable config                                        | Production deployment remains separately controlled and was not changed in Phase 07.                                                           | 07, 17             |
| Configuration/tooling  | Default scripts target Vite; `tsconfig.v2.json`, `lint:v2`, and `test:v2` validate TanStack.                                                                                  | Explicit `*:legacy` scripts and root Next TypeScript/config remain for frozen inspection.                                         | TanStack                                                  | npm, TypeScript, ESLint, Vite, Vitest                                  | Final legacy dependency/config removal remains Phase 14.                                                                                       | 14                 |
| Sanitization/utilities | `src/lib/sanitization.ts`, `src/lib/card.ts`, and `src/lib/slug.ts`.                                                                                                          | Similar helpers exist under root `lib/`.                                                                                          | TanStack                                                  | TypeScript, Zod                                                        | Phase 01 fixed the control-character escape, Wallet type mismatch, and centralized slug rules.                                                 | 06, 12, 15         |
| NFC                    | `/t/:token`, `public.nfc_tags`, and the narrow server resolver preserve immutable physical identity while redirecting to the card's current slug.                             | Legacy product copy contains no authoritative resolver.                                                                           | TanStack/Supabase                                         | Supabase, secure immutable tokens                                      | Lifecycle and assignment must remain admin/RPC controlled; public rendering must never expose raw inventory.                                   | 03, 05             |
| Demo system            | No authoritative demo system is allowed; Pro Demo Mode and public fake fallbacks were removed.                                                                                | One unrelated legacy Wallet API-key string check remains for Phase 12 migration.                                                  | Retired                                                   | None                                                                   | Remaining Wallet fallback behavior must not enter the TanStack public-card path.                                                               | 12, 14             |

## Configuration conflicts requiring deliberate resolution

- `package.json`: default scripts now run V2; explicit legacy aliases remain until Phase 14.
- `tsconfig.json`: includes legacy trees and excludes `src/**/*`; `tsconfig.v2.json` is the V2 validation config.
- `next.config.mjs`: static export to `out`.
- `wrangler.json`: expects the verified V2 `dist` artifact.
- `vite.config.ts`: TanStack Start/Nitro with Cloudflare Pages server output.
- `.lovable/project.json`: TanStack Start template.
- `src/routes/README.md`: states TanStack file routing and forbids `app/layout.tsx`, which still exists in the frozen legacy tree.

## Items intentionally retained after Phase 01

- all Next.js application files and dependencies;
- the legacy Wallet implementation and its static-parameter helper for Phase 12 migration review;
- duplicate Wallet, dashboard, and admin code scheduled for later phases;
- mutable `supabase/schema.sql` and current RLS policies.

They remain visible so later phases can migrate or remove them with evidence instead of assumption. Legacy public-card routes, renderer components, vCard route, and `c.html` redirect were removed in Phase 01.
